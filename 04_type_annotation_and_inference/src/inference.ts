// typescript automatically knows what is the type

let name = "arbaz"; // this is a type of string
// name = 123 => going to gives error

let thunder = Math.random() >= 0.2 ? "i know you" : undefined; // type of string|undefined
console.log(thunder);
